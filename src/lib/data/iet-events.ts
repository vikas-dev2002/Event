// Stub — seed data removed. Add event data here if needed for seeding.
export const ietLucknowEvents: {
  title: string;
  description: string;
  category: string;
  startDate: string;
  endDate: string;
  venue: string;
  capacity: number;
  tags?: string[];
  posterUrl?: string | null;
  documents?: unknown;
  customFields?: unknown;
}[] = [];
